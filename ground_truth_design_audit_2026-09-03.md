# Ground-Truth Design Audit — 280-combo 真实标定实验（2026-09-03）

> 范围：判断 140 cell × 2 q_p 档 = 280 combo 是否足以支撑主公式
> `q̂_r = W·c·(q_p/W)^p`（仅 c,p 两参数，q_r* 由 SUMO 扫描 + 30 seeds + per-seed 判据）。
> 证据来源：(a) 当前设计表（pipeline/cells/selected_cells.json，280 combo）；
> (b) 合成可辨识实验（audit_design.py，已知 c=27.17/p=-0.933、σ=1.5 robot/min、
> 离散化+截尾，400 次重采样）；(c) 归档 P1b 实测（200 combo / 26,340 扫流场景 /
> 30 seeds，同引擎同判据）。不修改代码，不重跑仿真。

## 0. 一个贯穿性事实：x = q_p/W 是离散的，不是连续的

q_p 由 `x_ctx × W` 定义（x_ctx ∈ {5,12,15,20,25,30,35,45}），因此 **x = q_p/W = x_ctx
几乎逐点等于语境常数**。280 combo 中 x 只取 8 个值，且 **76%（212/280）落在 {5, 12}**
（住宅 lo/hi），余下为 {15,25}（商业 62）与 {30,45}（站前 6）。corr(W,qp)=+0.982，
corr(W,x)=+0.937：宽度几乎决定了"流量"。**这是本设计的核心结构问题**，比"2 个档位"
本身更严重——连续幂律实际上是由 3–4 个 x 聚类的均值拟合出来的。

## 1. Two-level q_p identifiability

- 现状：每 cell 两档，hi/lo 比 1.5–2.5（语境先验决定）。
- 证据（合成实验，400 次）：2 档设计下 p̂ = −0.974 ± 0.054（真值 −0.933，偏差 −0.040
  来自离散化+截尾），95% CI 宽 0.213。**p 可被稳定识别**（因为 8 个 x 聚类跨 0.97 个
  数量级），但 CI ±0.1 意味着幂指数有实质不确定性（x=10 处配额 ×1.26/÷0.79）。
- 3 档（每 cell 几何中点）CI 收窄 10%；4 档收窄 19%——**边际收益小**。
- 结论：2 档在"能否识别 p"意义上**够**；但识别的是"过 3–4 个聚类均值的斜率"，
  不是连续幂律。档位数不是瓶颈，**q_p∝W 共线才是**。

## 2. q_p/W coverage

- x 分布：min 4.8 / p25 5.00 / 中位 11.99 / p75 12.03 / p90 25.0 / max 45.0。
- 聚类：x≈5: ~70，x≈12: ~106，x≈15: 31，x≈25: 31，x∈{30,45}: 6；33–41.7（容量边界
  邻域）**完全空白**，≥41.7 仅 3 个。
- 最大 log10 空档 0.37（5→12 之间）、0.22、0.18。
- 结论：覆盖集中在低-中段两个点；**容量边界邻域（x≈33）无数据**，无法从数据内
  标定 C(W) 边界；"连续幂律"声明不成立，当前数据支撑的是分段/聚类插值。

## 3. Width-flow confounding

- corr(W,qp)=+0.982，corr(W,x)=+0.937，corr(qp,x)=+0.945。
- 宽度分带 × x：窄带 x∈[4.8,5.0]（全部）、中带 [5,12]、宽带 [12,25]、超宽 [25,45]
  ——x 几乎被宽度带唯一决定。
- 后果：p 识别到的"流量效应"可能是宽度带的代理；W 与 q_p 的独立效应在数据中不可分。
  风险 **High**：这是能改变论文结论的结构性问题。

## 4. Ground-truth threshold resolution（归档 P1b 证据）

- 扫描阶梯 {0,1,2,3,4,5,6,8,10,12,15,18,20}：低段步长 1，高段步长 2–3。
  qr*∈{12,15} 时离散化误差可达 ±3 robot/min（相对典型 qr* 6–15 是 20–50%）。
- 43/137（31%）combo 的临界点位于相邻档之间（首个失败档 mean_Rv≥0.85 或
  pass_frac≥0.5）——阶梯在这些 combo 上只给出下界。
- 局部细化成本：43 combo × 1 档 × 30 seeds = **1,290 场景 ≈ 全量扫流的 5%**。
- 结论：离散化误差是最终模型误差的重要来源之一；**coarse scan + 边界局部细化
  性价比远高于加 cell**。

## 5. Seed / acceptance-rule stability（归档 P1b 证据）

- 判据敏感性：29/30 vs 均值判据 → 70/200 combo 改变；28/30 vs 29/30 → 仅 5 个改变。
  （per-seed 判据系统性更保守，与 rescore_seedwise 结论一致。）
- 种子子采样（均值判据，50 次重采样）：15 seeds → 22.4% combo 翻转；20 → 19.6%；
  25 → 17.4%。外推 30 seeds 仍约 ~15% 单档翻转率。
- 结论：30 seeds 是**下限**而非冗余；per-seed ≥29/30 在 30 seeds 下本身处于二项
  噪声边缘（1 个 seed 翻转即改变结果）。不建议减 seeds；建议论文报告 per-seed
  pass_frac 的置信区间。

## 6. Geometry residual effect（归档 P1b 证据）

- 86 对 (|ΔW|≤0.15, |Δqp|≤3, |Δqr*|≥4)：同 W/qp 下 quota 差 ≥4 台普遍存在
  （例：AMS W=2.50 qp=12，type A qr*=20 vs type B sin1.029 qr*=15；qp=30 时
  A=12 / B=6 / C=20）。
- log 残差均值：type C 0.466 / D 0.425 vs A 0.215 / B 0.210；城市间 London 0.474 /
  Amsterdam 0.438 vs Tokyo 0.096。
- 结论：两变量公式存在**明确遗漏变量风险**（曲率、瓶颈、路口前场、城市效应），
  且当前 280-combo 设计的几何多样性比 P1b 还低（B 型仅 8 cell、D 型 0）。

## 7. City generalization / leakage

- 同源重复：当前 140 cell 中每源最多 1 cell（无同路段重复）✓。
- 软泄漏：NLMA 同源（台北+新北训练 vs 桃园测试，同一测绘方法/字段体系）——桃园
  测试的独立性弱于 Seattle，论文须如实声明。
- LOO-city 可行性：5 训练城 → 5 折（80/20）可执行，每折 20 cell 偏小但够用；
  建议 LOO-city 做模型选择 + Seattle/Taoyuan 冻结外测，**不用随机切分**（同城
  cell 相关，随机切分高估泛化）。

## 8. Physical consistency（解析 + quota_params_final.json）

- ∂q̂/∂W = c(1−p)x^p > 0（p=−0.933 → 1−p=1.933）✓ 恒正；
  ∂q̂/∂q_p = c·p·x^(p−1) < 0 ✓ 恒负。单调性结构正确。
- 边界问题（裸公式）：q_p→0 时 x^p→∞ **发散**——必须靠 `Q_low(W)` 封顶（final 模型
  已含，源：qp=10 标签）；高 q_p 靠 x_crit/C(W) 归零 ✓；W<1.6 硬地板 ✓；
  宽走廊靠 q_max=20 ✓。
- 域外：final 模型适用域 W≤3.0、q_p≤60；当前设计含 W 最高 6.92 m、qp 最高 144
  （28 个超宽 combo + 站前高流）**超出标定域**——要么扩域，要么把超宽/高流作为
  out-of-domain 验证单独报告。
- 结论：护栏是承重墙（不是装饰）；论文必须把适用域写进算法规格，裸公式仅在
  护栏内有效。

## 结论表

| Issue | Current status | Evidence | Risk | Must fix before continuing? | Can fix after 280-combo scan? | Recommended minimum action |
|---|---|---|---|---|---|---|
| 1. 2-level identifiability | p 可识别但 CI ±0.1 | 合成实验 std 0.054；3/4 档仅收窄 10%/19% | Medium | 否 | 是 | 不增加档位；接受 ±0.1 并报告 CI |
| 2. x coverage | 76% 质量在 x∈{5,12}，边界邻域空白 | 280 combo 分布 | High | 否（扫描不浪费） | 是（补实验） | 补充非比例 q_p 档位（见 B） |
| 3. W–qp 共线 | corr=0.98，效应不可分 | 设计表 | High | 否 | 是 | 40 cell × 2–3 独立流量档解耦 |
| 4. 阈值分辨率 | 高段步长 2–3，31% combo 边界在档间 | P1b：43/137 borderline | Medium | 否 | 是 | borderline 局部细化（~5% 预算） |
| 5. seed 稳定性 | 30 seeds 是下限，~15% 单档翻转 | 子采样 15/20/25 → 22/20/17% | Medium | 否 | 是（报告口径） | 保留 30 seeds；报告 pass_frac CI |
| 6. 几何残差 | 同 W/qp 下 quota 差 ≥4 台普遍 | P1b：86 对；C/D 型残差 2×A/B | High | 否 | 是 | 论文中报告 type/city 残差，不增加变量 |
| 7. 泄漏/泛化 | 无同源重复；NLMA 软泄漏；LOO 可行 | 设计表 | Low | 否 | 是（零成本） | LOO-city + 冻结外测，弃随机切分 |
| 8. 物理一致性 | 单调性✓；q_p→0 发散需护栏；28 combo 出域 | 解析 + final.json | Medium | 否 | 是 | 适用域写死；超宽/高流作 out-of-domain |

## 三个问题

**A. 280-combo 扫描继续跑完，还是必须立即停改设计？**
继续跑完。没有任何发现要求停掉这次扫描——它仍是"真实几何真值"的主数据集，
且可断点续跑。但**必须追加补充实验**（见 B/C），否则论文只能声称"分段插值"，
不能声称"连续幂律"。当前运行处于暂停状态（仅损失 Phase A 前 ~2 分钟），恢复命令
不变（`python p1_multicity.py --n-procs 12`）。

**B. 2 个 q_p 档位够吗？不够的话最低成本补充是什么？**
"档位数"本身勉强够（合成实验：2 档可识别 p，3 档只再收窄 10%）；真正不够的是
**q_p∝W 的共线设计**。最低成本补充：从每训练城抽 8 个 cell（共 40 个，覆盖窄/中/宽），
对每个 cell 加 2–3 个**与 W 不成比例的流量档**（如住宅 cell 加 qp=30/60、商业 cell
加 qp=10/90 这类跨带档），40 × 2.5 档 × 30 seeds ≈ 3,000 场景 ≈ 1–2 小时，
即可打破共线、把 x 从 8 个聚类变成近连续，p 才真正代表流量效应。

**C. 不显著扩大预算，哪 1–3 个补充实验最能提可信度？**
1. **边界局部细化**（~5% 预算）：对 borderline combo 加 1 档扫描，消除高段 ±3
   robot/min 的离散化误差（P1b 显示 31% combo 受益）。
2. **40-cell 非比例流量档**（~1–2 h）：解耦 W–qp 共线，把幂律从"3 点插值"变成
   真流量响应——这是 B 的核心，也是性价比最高的结构性修复。
3. **零成本协议项**：LOO-city 验证 + Seattle/Taoyuan 冻结外测 + 逐城/逐类型残差表
   （把几何遗漏变量从"审稿人的质疑"变成"论文自己报告的已知边界"）。
