# 配送机器人道路配额算法 — 实验进度与下一步计划（2026-09-03）

> 本文档取代 2026-08-22 版（已归档至 `archive\2026-09-03_旧实验与旧数据\`）。
> 主线转向：**用 7 城真实人行道几何+实测宽度重新标定配额公式**，合成标定集降级为对照基线。

---

## 一、2026-09-03 当日工作总览

| 事项 | 状态 |
|---|---|
| 5 城人行道宽度数据下载与质检（NYC/Seattle/Melbourne/Amsterdam/Paris） | ✅ |
| 台北 NLMA 官方人行道数据核验（全国图资 2024-12 版） | ✅ |
| 7 城统一口径数据集（单位换算/宽度推导/QC/剔除） | ✅ `train_test_mapdata/` |
| 旧实验归档（experiment/experiment_sumo/sumo_jupedsim/巴黎/旧西雅图） | ✅ `archive/2026-09-03_旧实验与旧数据/` |
| 7 城标定测试管线（采样→q_p→驱动→烟测） | ✅ `pipeline/` |
| 全量 ground-truth 训练仿真 | 🔄 启动中（140 cell / 280 combo / 30 seeds，预计 14–16 h） |

## 二、数据资产

### 2.1 统一数据集（`train_test_mapdata/`）

训练：NYC / Amsterdam / Melbourne / Taipei / New Taipei；测试：Seattle（主）/ Taoyuan（次）。
统一字段 `width_m`（米）、`net_width_m`、`width_source`（实测/推导标注）、`width_qc`、`city`/`city_cn`/`split`。
详见目录内 `DATA_SOURCES.md`（来源、单位、处理决策、已知质量问题全记录）。

| 城市 | 要素 | 宽度中位(m) | 宽度性质 |
|---|---|---|---|
| NYC | 464,956 | 2.87 | 社区项目从 2024 平面测量面推导（英尺→米） |
| Seattle | 46,085 | 1.52 | 官方 SDOT 资产普查（英寸→米，全量 46k 重下，过滤 INSVC/PLNRECON） |
| Amsterdam | 217,785 | 2.33 | 官方 BGT 推导（仅 bgt_* 类边有宽度） |
| Melbourne | 25,453 | 1.97 | 2×面积/周长推导（两层合并；仅市辖区） |
| Taipei | 18,060 | 1.82（净宽 1.36） | 官方 NLMA 实测 |
| New Taipei | 13,981 | 2.01 | 官方 NLMA 实测（剔除 3 条 >60 m 录入错误） |
| Taoyuan | 10,198 | 2.52 | 官方 NLMA 实测 |

巴黎文件无宽度字段，已归档（`archive\...\paris_无宽度字段\`，需要时另找宽度源）。

### 2.2 候选池与选中 cell（`pipeline/cells/`）

- 候选池 7 城共 32.9 万 cell（线城切 50 m 段；面城凸包弦；阿姆斯特丹按 startNodeId/endNodeId 链式串接，转向≤60°、宽度按长度加权）。
- 分层抽样 **140 cell**（每城 20：窄<1.6 m ×7 / 中 1.6–2.4 ×7 / 宽 2.4–3.5 ×4 / 超宽 ≥3.5 ×2；seed=42，同源≤2，间距≥100 m），全部严格 50 m。
- q_p 语境标注（Overpass POI + P1b 分类器）：住宅 106 / 商业 31 / 站前 3；`q_p_peak = x_ctx × W_eff`。

## 三、管线（`pipeline/`）

`sample_cells.py → select_cells.py → download_poi.py → annotate_flow.py → project_cells.py → sim/`

- 仿真驱动为旧 experiment_sumo 脚本副本（`sim/scripts/`，31 个文件），cells.json 路径已适配；
  SUMO 1.27.1 + JuPedSim 在 `C:\Program Files (x86)\Eclipse\Sumo`（SUMO_HOME 环境变量）。
- 烟测通过并修复两个真 bug：
  1. **中心线坐标口径**（经纬度 vs 米）致测速区采样恒空 → `project_cells.py` 投影为局部米制；
  2. 阿姆斯特丹链式 cell 尾段合并致长度 50–90 m → 严格 50 m。
- 修复后烟测物理行为与 P1b 标定一致（W=1.68/qp=8 → qr*=10）。

## 四、正在运行 / 下一步

1. **全量 ground-truth**（`python pipeline\sim\scripts\p1_multicity.py --n-procs 12`）：
   140 cell × 2 q_p 档 × 30 seeds = 280 combo，预计 14–16 h，可断点续跑（`--skip-sim`）。
2. 跑完后：`rescore_seedwise_full.py` per-seed 重打分 → **真实数据主模型拟合**
   （5 城训练 + LOO-CV；合成标定集仅作 synthetic-only 对照基线，不进训练池；
   Seattle/Taoyuan 留出测试；W<1.0 m cell 标 physically_impassable 剔除）。
3. 拟合/测试协议（冻结）：seeds=30、per-seed ≥29/30 主判据（mean 为上界）、q_max=20、
   测试前冻结模型与协议、事后不改。

## 五、关键文件索引（2026-09-03 版）

- 数据：`train_test_mapdata/`（7 城 geojson + DATA_SOURCES.md + manifest.csv）
- 管线：`pipeline/`（README.md 为管线状态与协议；cells/ 采样产物；osm/ POI；sim/ 仿真）
- 旧实验（归档）：`archive/2026-09-03_旧实验与旧数据/`（含 experiment_sumo 全部产物、
  论文模型 `quota_params_final.json`、P1b 多城结果、旧进度文档）
- 设计文档：`配送机器人_研究方向_真实数据标定_2026-09-03.md`（本轮实验设计）
- 历史设计：`配送机器人_研究方向_Quota算法更新版.md`、`配送机器人_实验计划书_SUMO_JuPedSim_新加坡.md`
