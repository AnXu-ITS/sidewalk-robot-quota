# 换机迁移说明（2026-09-04，暂停时状态）

## 当前状态

- 主扫描（280 combo）已暂停；Phase A 未完成（基线 CSV 只在阶段结束时写出），
  **换机后从头启动即可，无需恢复**（没有可续的中间状态）。
- 超时修复（`SIM.timeout_sec=300` + 保守失败语义）已在代码内，随拷贝一起迁移。
- 所有管线脚本、40-cell 选择、120-combo decoupled 设计、拟合/LOCO/细化/看门狗
  全部就绪并验证过；换机后只需跑仿真 + `run_all_analysis.py`。

## 压缩包内容（quota_law_experiment_kit_2026-09-04.zip）

解压后得到：

```
pipeline\                     # 全部管线：10+ 脚本、冻结设计（selected_cells.json /
                              #   selected_40_cells.json / decoupled_cells.json /
                              #   refinement 相关）、osm POI、sim\scripts（驱动+修复）
train_test_mapdata\           # 7 城统一数据（train_*.geojson / test_*.geojson）
                              #   + README.md + manifest.csv + build 脚本
quota_params\                 # 护栏参数 quota_params_final.json（fit_models.py 读取）
换机迁移说明.md / README.md 等 # 文档
```

**不在包内（可再生，无需拷贝）**：`pipeline\sim\outputs\`（仿真临时输出）、
`pipeline\cells\cells_*.geojson`（候选池，`sample_cells.py` 可由 7 城数据 2 分钟重建）、
`train_all.geojson`/`test_all.geojson`（合并文件，`build_train_test_mapdata.py` 可重建）。

## 新机器要求

1. **Python 3.10+**：`pip install numpy pandas scipy scikit-learn ijson`
2. **Eclipse SUMO 1.27.1（含 JuPedSim）**：把 `sumo.exe/netconvert.exe` 所在目录
   加入 PATH，并设置环境变量 `SUMO_HOME`（config.py 优先读它）。
3. 内存 ≥ 32 GB 建议（12 workers × SUMO 子进程）。

## 新机器上的启动顺序

```powershell
# 0) 验证环境
sumo --version          # 应显示 1.27.1 ... JuPedSim

# 1) 主扫描（280 combo / 30 seeds / 12 workers，约 14–16 h，可断点续跑）
cd <新路径>\pipeline\sim\scripts
python p1_multicity.py --n-procs 12
python p1_multicity.py --skip-sim        # 中断后接着跑（按阶段/按档复用 CSV）

# 2) 补充扫描（120 decoupled combo，约 6–7 h）
python p1_decoupled.py --n-procs 12
python p1_decoupled.py --skip-sim

# 3) 边界细化（自动识别 borderline，约 1–2 h）
cd <新路径>\pipeline
python refine_boundary.py --plan
python refine_boundary.py --run

# 4) 一键分析（产出全部规定 CSV + 报告所需数据）
python run_all_analysis.py
```

## 注意

- 看门狗（`watch_scan.py`）可后台常驻：`python watch_scan.py`（崩溃自动 --skip-sim 重启）。
- qp=75 等病理组合会触发 300 s 超时并被保守判失败（`timed_out=1`），属预期行为，
  不要因此怀疑仿真坏了。
- 报告：`pipeline\quota_law_experiment_report.md`（含 Q1–Q6 草案与执行事故记录），
  数据齐后填入最终数字。
