# 配送机器人道路配额算法 — 实验进度与下一步计划（2026-08-22）

> 本文档为项目实验状态的快照式检查报告，涵盖 `experiment/`（纯 Python 社会力 MVP）与
> `experiment_sumo/`（SUMO–JuPedSim 主线）两条实验线，并给出按优先级排序的下一步计划。
> 所有数据均来自实际运行产出的 CSV / 报告，未做手工编造。

---

## 一、总体进度判断

两条实验线都已跑通并产出**真实结果**：

| 目录 | 引擎 | 状态 |
|---|---|---|
| `experiment/` | 纯 Python 社会力模型（MVP） | ✅ 全流程完成 |
| `experiment_sumo/` | Eclipse SUMO 1.27.1 + JuPedSim（`--pedestrian.model jupedsim`） | ✅ 全流程完成，当前主线 |
| `sumo_jupedsim/data/osm/bendemeer_road.osm` | — | ✅ 已下载真实路网，**已用于 P1 选址 / 人流推算 / 几何管线** |

- 已确认 SUMO 1.27.1（含 JuPedSim）已安装可用；`experiment_sumo` 确实调用了真实 SUMO
  二进制（存在 `netconvert.log`、`cell.net.xml` 等产物）。
- 计划书 Phase 0/3/4/5/6/7/8/9 的**技术链已全部打通**。
- 核心报告：`experiment_sumo/reports/experiment_report.md`（主线结论以该文件为准）。

---

## 二、核心结果（SUMO–JuPedSim 主线）

### 1. Ground-truth quota frontier（8 宽 × 6 流，30 seeds，P0 解饱和后）

数据：`experiment_sumo/outputs/quota_labels/quota_dataset.csv`

| qp↓ \ W→ | 1.5 | 1.6 | 1.7 | 1.8 | 2.1 | 2.4 | 2.7 | 3.0 |
|---|---|---|---|---|---|---|---|---|
| 10 | 0 | 6 | 10 | 12 | 15 | 20† | 20† | 20† |
| 20 | 0 | 5 | 6 | 6 | 12 | 15 | 20† | 20† |
| 30 | 0 | 3 | 5 | 4 | 8 | 12 | 18 | 18 |
| 40 | 0 | 0 | 0 | 1 | 2 | 12 | 15 | 15 |
| 50 | 0 | 0 | 0 | 0 | 0 | 6 | 10 | 10 |
| 60 | 0 | 0 | 0 | 0 | 0 | 4 | 6 | 6 |

`†` = `above_upper`（仍卡在 20 扫描天花板，读作"≥20"）。单调性基本成立 ✓；
唯一例外 `(W=1.7, qp=30)=5` vs `(W=1.8, qp=30)=4` 为 1 档的 seed 噪声。
P0 把饱和单元从 17 个降到 5 个，并把 `W_min` 从 1.8 m 重标定到 **1.6 m**
（1.6 m 实测可运行 `q_r*=6`、1.7 m 实测 `10`，1.5 m 仍全为 0）。

### 2. 配额算法拟合

数据：`experiment_sumo/data/processed/fit_metrics.csv`，参数：`experiment_sumo/models/quota_algorithm/quota_params.json`

- **Model A**（宽度归一化幂律）：`q̂_r = W · 28.22 · (q_p/W)^−0.828`，
  `x_crit = 33.3 ped/min/m`，`W_min = 1.6 m`（P0 前为 9.64 / −0.480 / 1.8 m）
- 训练集 MAE：A = 2.39 / B = 2.25 / **A-iso = 1.41**；OAR：A-iso = 0.292
  （P0 前为 A=1.80 / A-iso=1.11 / OAR 0.278 —— 详见 §三"去偏置代价"）

### 3. 留出验证 + 闭环 SVR

数据：`experiment_sumo/outputs/validation/`

- 未见宽度（1.65–2.85 m）MAE：A = 3.02 / B = 2.82 / **A-iso = 2.71**；A-iso OAR = 0.229
- 闭环服务违规率：**A-rounded = 0.176（6/34）**、A-floor = 0.118（4/34）、
  A-iso = 0.147（5/34）

### 4. 敏感性

- 阈值 δ_v ∈ {5% / 10% / 15%}：三档配额面已生成
  （平均配额 5%→4.9，10%→5.9，15%→6.4 robot/min）
- 机器人速度 ±20%：配额**完全不变**（最大变化 = 0）✓

### 5. 超出计划的增量产出：PRE（Pedestrian Replacement Equivalent）

`experiment_sumo/scripts/pre.py` 量化"1 台机器人 ≈ 多少行人"：

- W = 1.5 m：1 台 ≈ **95 行人**
- W = 1.8 m：1 台 ≈ 6.4 行人
- W = 2.1 m：1 台 ≈ 17.1 行人
- W = 2.4 m：1 台 ≈ 3.7 行人
- W = 2.7 m：1 台 ≈ 2.6 行人
- W = 3.0 m：1 台 ≈ **2.3 行人**

且随方向分流比变化（非恒定值）。这是很好的论文增量贡献。

### 6. 图表

`experiment_sumo/outputs/figures/` 已生成 9 张图：degradation、quota frontier、
algorithm-vs-reference（A/B/A-iso）、policy table、threshold sensitivity、
speed sensitivity、PRE surface。

---

## 三、需要正视的问题（影响论文可信度）

> P0 已修复问题 1、2（见 §四）；但去偏置后暴露出一个更深的结构性问题（问题 2'）。

1. ~~**上界饱和**~~ → ✅ **已修复**：扫描上限 10→20 robot/min，饱和训练单元 17→5 个
   （残留在 W≥2.4 且 qp≤20 的 5 个最轻单元，仍标 `above_upper`；20 robot/min 已接近
   50 m 走廊的运营上限，可接受，或后续再扫到 25–30）。

2. ~~**`W_min=1.8` 硬截断悬崖**~~ → ✅ **已修复**：补入 1.6/1.7 m 训练宽度后 `W_min`
   重标定为 1.6 m，W=1.65 测试单元配额恢复为 `q_r*=8/6/5/5/3`（此前被全 0）。

2'. ~~**全局 `x_crit=33.3` 无法刻画"窄走廊更早过载"**~~ → ✅ **已修复（P0'，容量约束）**：
   原 6 个闭环违规全部集中在 W=1.65/1.95 的"窄 × 高流"区（如 1.65/45 真实 0、Model A
   预测 3.0）。现已引入 **W 依赖的机器人可容行人流量容量 `C(W)`**（数据标定、对 W 单调）：
   `q_p ≥ C(W)` 时配额归零，替代单一全局 `x_crit` 对窄走廊的失效；并加 `q_max=20`
   验证上限压平宽端发散。违规从 6 降到 2（残留 1.65/27、1.95/35 均为边界噪声，
   `mean_Rv≈0.86–0.90` 恰在 0.9 判据线上）。

3. **SVR 违规集中定位**（已随问题 2 修复而迁移）：P0 前 3 个违规在 W=1.95/35/45/55；
   P0 后 6 个违规 = 1.65/27/35/45 + 1.95/35/45/55，即窄走廊高流过渡区（见问题 2'）。

4. **判据降级**：计划书要求 per-seed `Pr(C_p=1)≥0.95`，实现里退化为 **mean-based**
   主判据（低流时称"被到达噪声主导"）。论文里需严格论证或补足 seed。

5. ~~**真实场地缺失**~~ → ⚠️ **部分解决（桌面替代现场）**：无法现场核验，已用 OSM + 网上
   信息完成 Bendemeer 选址（27 个真实 50 m cell）、`W_eff`/`q_p` 推算、netconvert 转网
   与几何管线（`simulator.py` 已支持真实折线走行区）。但宽度/人流为**估计值非实测**，
   论文须标注为 prior（见 §四 P1）。

6. **机器人是圆盘不是定向矩形**：JuPedSim CollisionFreeSpeedModel 把 0.96×0.70 m 压成
   0.48 m 圆盘（报告已诚实披露）。

7. **跨引擎差异未比对**：`experiment/`（Python 社会力）与 `experiment_sumo/` 在高密度
   区结果差异很大（社会力模型保守得多），尚未并排说明。

---

## 四、下一步计划（按优先级）

### P0 — 修复直接影响结论的问题（✅ 已完成，2026-08-22）

1. ~~**解除上界饱和**~~ → ✅ 已做：`extend_experiment.py` 对饱和单元扫到 20 robot/min，
   饱和训练单元 17→5，重聚合/重拟合/重验证/重闭环全部重跑。
2. ~~**替换 `W_min` 硬截断**~~ → ✅ 已做：补入 1.6/1.7 m 训练宽度，`W_min` 重标定为
   1.6 m；W=1.65 悬崖消除。结论：SVR/OAR **没有下降反而上升**（见 §三问题 2'），
   因为去偏置后暴露了更深的容量结构问题。

### P0' — 结构重构（✅ 已完成，2026-08-22）

1. ~~**W 依赖的容量约束**~~ → ✅ 已做：`fit_quota.py` 新增 `estimate_capacity()`，从 sweep
   数据标定 `C(W)`（对 W 单调的 piecewise-linear，保守取"最后可运行/首次不可运行"中点），
   预测改为 `q_p < C(W)` 才放行；`quota_params.json` 存 `C_W` 节点。
2. ~~**宽端平顶**~~ → ✅ 已做：`q_max = 20 robot/min`（已验证扫描上限）封顶，消除
   W≥2.4 低流时的发散过冲（如 2.85/12 预测 24.5→20）。
3. ~~**重跑验证**~~ → ✅ 已做，结果（训练 / 留出 / 闭环）：

   | 指标 | P0' 前 | P0' 后 |
   |---|---|---|
   | 训练 MAE（A / B / A-iso） | 2.39 / 2.25 / 1.41 | **1.53 / 1.19 / 1.30** |
   | 训练 OAR（A / B / A-iso） | 0.479 / 0.438 / 0.292 | **0.271 / 0.208 / 0.188** |
   | 留出 MAE（A / B / A-iso） | 3.02 / 2.82 / 2.71 | **2.65 / 2.30 / 2.75** |
   | 留出 OAR（A / B / A-iso） | 0.314 / 0.286 / 0.229 | **0.171 / 0.114 / 0.171** |
   | 闭环 SVR（A-round / A-floor / A-iso） | 0.176 / 0.118 / 0.147 | **0.059 / 0.029 / 0.118** |

   目标达成：SVR/OAR 全部拉回 P0 前水平以下，同时保住去偏置后的真实 frontier；
   **A-floor（保守向下取整）现为最安全侧部署规则（SVR=0.029，1/34）**。

### P1 — 补齐真实场地（计划 Phase 1–2）（✅ 已完成桌面替代，2026-08-22）

1. ~~**地图与选址**~~ → ✅ 已做：`bendemeer_road.osm` + `bendemeer_poi.osm` 覆盖 Bendemeer
   走廊（北 Boon Keng NEL ↔ 南 Bendemeer DTL 两地铁站间换乘步行段）；`analyze_osm.py`
   筛出 27 个真实 50 m cell（`sumo_jupedsim/data/cells.json`）。
2. ~~**人流推算（无现场核验）**~~ → ✅ 已做：`estimate_site_flow.py` 按用地 + 地铁邻近度
   推算 `W_eff`（1.5–3.0 m）与 `q_p`（住宅 8–20 / 商业 34–56 / 地铁前场 78–117 ped/min）。
   结论：住宅/商业段落在已标定区间内；**地铁前场峰值超出扫描上限 → 站前高峰 `q̂_r=0`**。
3. ~~**几何管线**~~ → ✅ 已做：`netconvert --osm-files` 转 `bendemeer.net.xml`（1227 条行人
   边）；`build_cells.py` 裁真实 cell；`simulator.py` 支持真实折线走行区 + 沿程距离度量
   （`run_scenario(..., centerline=...)`，向后兼容，且修复了 netconvert 坐标归一化错位）。
4. ~~**P1 ground-truth 扫描**~~ → ✅ 已跑完（`p1_real_site.py`，6 cell × 2 流量档 × 30
   seeds，早停 sweep；结果 `outputs/p1_real_site/p1_results.csv`，报告
   `reports/p1_real_site_report.md`）。**违规计数：A-round 6/12、A-floor 3/12、A-iso 1/12**。
5. **P1 核心结论**：
   - **稠密真实 cell 上算法过度分配**：地铁前场 `qp=78`（x=30 < x_crit=33.3）算法预测
     q̂=4.4，仿真真值 `q_r*=0`（加 1 台即崩）→ 真实几何有效容量低于合成直线标定值。
   - **A-floor 仍最安全**（25% vs 50%），与合成闭环一致。
   - **曲率：本走廊本质全直线**（27 cell 的 sinuosity 全在 1.0000–1.0024，最大转角
     17°/50 m）；此前"曲线 vs 直线 −25%"为 seed 噪声误读，已更正。
   - 轻载段（商业 qp=34）保守欠分配 −46%（安全但低效）。
6. ~~**重标定 x_crit/C(W) + 曲率惩罚**~~ → ✅ 已做（`p1_capacity.py` 容量扫描 +
   `recalibrate_p1.py`）：
   - 实测 `C_real(2.25)=63`、`C_real(2.6)=76`、`C_real(1.65)>48`；合成 `C(W≥2.4)=90`
     是截断值被证实过松。
   - 新模型 `models/quota_algorithm/quota_params_p1.json`：`x_crit 33.3→29.0`、
     `C(W)` 替换截断段为 2.25→63、2.6→76、3.0→76（保序）；曲率惩罚
     `W_c=W_eff/sinuosity`（本走廊 no-op）。
   - **复验违规：A-round 6→4、A-floor 3→1、B-floor 3→1、A-iso 1→1**；地铁 qp=78 的
     q̂_A 4.39→0（修复灾难性过度分配）；剩余违规 ≤1.5 台（幂律幅度，非容量截断）。
7. **待做**：重拟合幂律幅度（c,p）以消除剩余小幅违规（可选）；补 Type-B/C 外部验证。
   → **已启动（见 §P1b，2026-08-24）**。

### P1b — 多城市外部验证 + P1 收尾（✅ 已完成，2026-08-24）

把验证面从 Bendemeer 单点扩到四城（新加坡/伦敦/东京/阿姆斯特丹），产出 P1 收尾所需的
Type-B/C 外部验证与合并重拟合数据。**全流程已跑完（ground-truth 10.5 h + 分析）。**

1. **多城市训练集构建** ✅：`download_osm.py`（修复 road 查询 `(._;>;); out body;` 保节点
   标签）+ `analyze_osm.py`（A/B/C/D 判别）+ `build_cells.py`（§2 预算分层选取）产出
   `data/cells.json`（100 cell 带 city 字段）+ `cell_coverage.md`。
   覆盖：A/B/C/D = 40/20/25/15 全达标；窄 19/20（东京贡献 13，path 默认 1.2 m）、
   中 23/25、宽 18/20、超宽 5/10（可选档）。伦敦瓶颈 C=9、阿姆斯特丹曲线
   sin 1.03–1.23（真实运河弯，已过滤闭环广场垃圾）。
2. **POI 语境精化** ✅：`estimate_site_flow.py` 参数化（读 cells.json，用各城
   `<city>_poi.osm` 的 shop/amenity/landuse/railway 分类，新增 §5"旅游/混行"档）。
   语境分布（`context_coverage.md`）：住宅 45 / 商业 20 / 前场 14 / 旅游混行 21
   （vs §2 ~35/30/25/10；缺口如实反映采样区构成，非塌陷）。
3. **ground-truth 驱动器** ✅：`p1_multicity.py`（四城全 cell、分层抽样 `--limit`、
   30 seeds、早停扫流、断点续跑）；`fit_multicity.py`（合成+真实合并重拟合 (c,p)、
   三城训练+阿姆斯特丹留出、Type-B/C 验证），干跑通过。
4. **顺带修复 2 个 bug** ✅：numpy 2.x `clip(lower=)`→`clip(min=)`；`fit_quota.py::
   estimate_capacity` 的 PAV 等渗回归压缩 C(W) 数组长度与 W 失配（合成数据恰单调从未
   触发，合并真实数据必崩）→ 改 `maximum.accumulate`。
5. **ground-truth 仿真** ✅：全量 100 cell × 2 流量档 × 30 seeds = 200 combo（6000
   基线场景）后台分批（12 workers），实际 ~10.5 h 跑完，exit 0。结果
   `outputs/p1_multicity/multicity_results.csv`：82/200 非零配额、17 触顶 q_max=20、
   25 Case-1（行人过载归零）。
6. **P1 收尾分析** ✅（`fit_multicity.py --real`，详见 `experiment_report.md` §8）：
   - **W_min=1.6 强验证**：W<1.6 的 106 个 combo 中 104 个 qr*=0（窄巷/东京 1.2 m
     path 确实不放机器人）。
   - **模型迁移良好**：P1 模型（合成 c/p）在 200 真实 combo 上 MAE=1.286、OAR=0.090、
     floor 过分配 16/200（其中 8 个仅超 1 台；5 个超 4–5 台，集中在 W≥3.6 超宽 cell
     被 q_max=20 封顶 + 阿姆斯特丹曲线 cell 离散失效）。
   - **合并重拟合不采纳**：合成+真实重拟 (c,p)=(32.2,−0.856) 反而把过分配 16→27
     （OAR 0.090→0.145）——残留违规根源是 q_max 封顶与曲线离散失效，**非幂律幅度**，
     故保留合成 c/p。
   - **三城训练+阿姆斯特丹留出**：留出 MAE=2.917（加曲率惩罚 2.851）、OAR=0.222、
     floor 8/36；留出城（运河弯 B cell + 4.0 m 滨海道）迁移退化。
   - **Type-B/C**：C（瓶颈）安全（2/50=4% 过分配）；B（曲线）需曲率惩罚
     （7/40→5/40，17.5%→12.5%），但中等流量曲线上仍有离散失效 → **适用域为
     直线～轻弯走廊，锐弯需离散归零处理**。
7. **数据质量注记（2026-08-24）**：
   - `SIN-1189801155-0`（C，`width=0.5` 真实窄缝）机器人直径 0.96 m > 0.5 m，机器人
     物理不可通行；单场景实测 JuPedSim **不崩溃**（返回 0 流量退化指标，qr* 经 vbar=0
     路径正确判 0）。该 cell 属"机器不可通行"排除范畴，非楼梯桥。
   - `fit_multicity.py` 的重拟合改为**只重拟 (c,p)**，`x_crit=29 / W_min=1.6 / C(W)`
     继承 P1 实测容量结构（`fit_cp()`），避免退化 0 流量 cell 抬高 x_crit。
   - **AMS-244428233 宽度标签复查 ✅**（待办 C，2026-08-24）：OSM way 244428233「Kop van
     Jut」仅 `highway=path` + `surface=asphalt` + `name`，**无 `width`/`sidewalk` 标签**，
     `W_eff=1.2 m` 是 OSM `path` 默认值非实测（铺装沥青路真实宽度大概率 ≥2 m）。它是唯一
     W<1.6 却 qr*=20 的 cell（qp=6/14 两 combo）；W_min=1.6 地板把它保守归 0（欠分配 20），
     属**宽度标签低估**，非算法缺陷。建议：按航拍宽度重标注或剔除 width-untagged `path`。

### P2 — 方法学加固

1. ~~恢复 per-seed `Pr(C_p=1)≥0.95` 判据~~ → ✅ 已做（2026-08-24，`rescore_seedwise.py`
   + `experiment_report.md` §8.6）：从**已有 sweep CSV 重打分**（无需重跑仿真）。per-seed
   判据（≥29/30 seed 单点通过）比均值判据**严格且单调更保守**：70/175 combo 下调（无一上调），
   总配额 1028→753（**−27%**），非零 82→78；下调集中在**低流 qp=9–12**（到达噪声主导）。
   决策：per-seed 为正确服务保证 ground-truth（论文主判据），均值 qr* 为上界，两者并报。
2. ~~跨引擎一致性表~~ → ✅ 已做（2026-08-24，`cross_engine.py` + `experiment_report.md`
   §8.5）：把 `experiment/`（Python 社会力）与 `experiment_sumo/`（JuPedSim）frontier
   并排，同口径重拟合。结论——**结构稳健、参数引擎相关**：frontier 排序 Spearman ρ=0.915，
   W_min/x_crit 截断一致；但幅度 2.79×、指数 0.72×（社会力引擎更保守，p=−1.19 vs −0.86）。
   W_min 差异（1.8 vs 1.6）是网格覆盖伪影（社会力未测 1.6–1.7m）。部署含义：算法形式可迁移，
   仅 (c,p) 需按引擎重标定；社会力=保守下界、JuPedSim=乐观上界。
3. ~~补 Type-B/C 外部验证~~ → ✅ 已在 P1b 完成（C 安全 4%，B 需曲率惩罚）；**P2 加固
   已做**（2026-08-24，详见 `experiment_report.md` §8.4）：
   - 修掉转角度量 bug（零长度段产生伪 `max_turn_deg`，8/100 cell 修正，无需重跑仿真）；
   - **转角×宽度迷你实验**（`bend_sweep.py`，θ∈{0,30,45,60,75,90}°×W∈{2,2.5,3}m×qp50×30
     seeds=18 combo）揭示：**单点尖角（哪怕 30°）就把机器人吞吐从 8–10 打到 0–2**，判别
     量是转角集中度 `max_turn/cum_turn≈1`（单角）vs ≤0.5（渐进弯），sinuosity 抓不住；
   - 保守 guard 据此升级（`max_turn≥20° 且 cum≥20° 且 集中度≥0.6 → q̂=0` +
     `W>3.0→q_max=15`，`quota_params_p2.json`）：over_floor 14→12、max_over 5→4，代价
     under_floor 62→63（**未采纳为正式模型**，样本缺单点尖角，价值在保护未来路口角）；
   - 待办：**角位置扫 + 真实路口角采样**（`bend_position_sweep.py`，entry/middle/exit ×
     15/20/30/45/60/75/90°，W=2.5/qp50/30 seeds=22 combo，跑中）；真实路口角筛查已做——
     100-cell 样本 W≥1.6 的集中单角（max_turn≥20 且 cum≥20 且 conc≥0.6）仅 3 个
     （AMS-1351790552 qr*=0 ✓、SIN-1105987650 qr*=6 保守归零、TYO-184298559 Case-1 已归零），
     **真实路口角需新 OSM 采样**（记为后续）。

### P3 — 论文产出

1. 用现有 `fig_policy_table.png` 出三档政策表（δ_v=5/10/15% 对应
   strict/normal/permissive），开始撰写论文 Method + Results 章节。

---

## 五、关键文件索引

- 研究方向：`配送机器人_研究方向_Quota算法更新版.md`
- 实验计划：`配送机器人_实验计划书_SUMO_JuPedSim_新加坡.md`
- 主线报告：`experiment_sumo/reports/experiment_report.md`
- 校准报告：`experiment_sumo/reports/calibration_report.md`
- 配额数据集：`experiment_sumo/outputs/quota_labels/quota_dataset.csv`
- 拟合指标：`experiment_sumo/data/processed/fit_metrics.csv`
- 验证指标：`experiment_sumo/outputs/validation/test_metrics.csv`
- 闭环 SVR：`experiment_sumo/outputs/validation/svr_summary.csv`
- 模型参数：`experiment_sumo/models/quota_algorithm/quota_params.json`（合成）→
  `quota_params_p1.json`（P1 真实重标定）
- 图表目录：`experiment_sumo/outputs/figures/`
- 真实路网：`sumo_jupedsim/data/osm/bendemeer_road.osm`（+ `bendemeer_poi.osm`、`bendemeer.net.xml`）
- 选址与推算：`sumo_jupedsim/data/site_selection.md`、`site_flow_estimate.md`、`cells.json`
- 几何管线脚本：`sumo_jupedsim/scripts/{analyze_osm,estimate_site_flow,build_cells}.py`
- P1 真实场地扫描：`experiment_sumo/scripts/p1_real_site.py` → `outputs/p1_real_site/p1_results.csv`
- P1 容量扫描/重标定：`experiment_sumo/scripts/{p1_capacity,recalibrate_p1}.py` →
  `outputs/p1_real_site/p1_recalibrated.csv`
