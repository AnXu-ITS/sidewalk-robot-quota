# 配送机器人道路配额算法 — 真实数据标定设计（2026-09-03）

> 本文档为新一轮实验的设计说明：**用 7 城真实人行道几何与实测宽度重新标定配额公式**。
> 前代设计（合成直线走廊标定 + Bendemeer 实测重标定 + 4 城 OSM 验证）见
> `配送机器人_研究方向_Quota算法更新版.md` 与已归档的实验报告。

## 1. 动机

前代模型 `q̂_r = W·c·(q_p/W)^p` 的参数 (c,p) 与容量结构 C(W)/x_crit 全部来自**合成直线走廊**
仿真（8 宽 × 6 流网格），真实场地的宽度/人流是 OSM 标签或桌面推算值（prior，非实测）。
审稿风险明确：标定仍依赖理想化几何。本轮用**官方公开的人行道几何+宽度数据**构建真实
cell，在真实几何上重跑 ground-truth 仿真并重新拟合，把标定依据升级为真实城市数据；
合成标定集降级为对照基线。

## 2. 数据与划分

| 角色 | 城市 | cell 数 | 宽度来源 |
|---|---|---|---|
| 训练 | NYC / Amsterdam / Melbourne / Taipei / New Taipei | 5 × 20 = 100 | 官方实测（Taipei 系 NLMA、Amsterdam 系 BGT 推导）+ 推导（Melbourne 2A/P、NYC 航测面推导） |
| 测试 | Seattle（主）/ Taoyuan（次） | 2 × 20 = 40 | 官方实测（SDOT / NLMA） |

- 每城分层：窄<1.6 m ×7 / 中 1.6–2.4 ×7 / 宽 2.4–3.5 ×4 / 超宽 ≥3.5 ×2；严格 50 m。
- 全部 cell 的中心线投影为局部米制（供仿真），原经纬度存 `centerline_ll`。
- q_p 为 POI 语境先验（`x_ctx × W_eff`，住宅/商业/站前/旅游混行四档），非现场实测——
  论文中如实标注；宽度是实测或推导，q_p 是 prior，二者来源分开声明。
- 划分依据：Seattle 是全数据集唯一官方资产普查宽度 → 测试标签最干净，做主测试；
  Taoyuan 与训练城同源（NLMA），作省内跨城次测试（其独立性弱于 Seattle，论文如实说明）。

## 3. 方法流程

1. **真实几何 ground-truth**：`p1_multicity.py`（SUMO 1.27.1 + JuPedSim）对每个
   (cell, q_p) combo 跑基线 + 升序机器人扫流（0→20 robot/min，early-stop），
   30 seeds，per-seed ≥29/30 通过为主判据（mean 为上界），得 q_r*。
2. **重拟合**：训练城 200 combo 上拟合 (c,p) 与容量结构；公式形式沿用
   「准入护栏（W_min/x_crit/C(W)/锐角/vbar）+ 有界幂律 + floor」两层架构，
   仅参数重标定。LOO-CV（5 城轮换留出）选超参。
3. **留出测试**：Seattle（主）/ Taoyuan（次）40 cell 一次跑完、只测一次。
4. **合成标定集角色**：
   - 对照基线（旧 `quota_params_final.json`，synthetic-only）——论文三列对比
     synthetic-only / real-only / mixed；
   - 覆盖补丁（可选）：仅当真实数据在 (W, q_p) 网格存在缺口时，基于**训练集覆盖分析**
     决定补丁，触发规则在测试前写死，作为敏感性分析单独报告；**不进主模型**。

## 4. 协议（冻结，事后不改）

- seeds=30；q_max=20；判据 per-seed ≥29/30（主）/ mean（上界）。
- 模型、代码、协议在测试前冻结；测试结果只读。
- W<1.0 m cell 标 physically_impassable（机器人 0.96 m 物理不可通行），拟合剔除。
- 语境构成（住宅 106/商业 31/站前 3）为本次选样真实分布，报告时如实标注。

## 5. 预期产出与审稿定位

- 主结果：真实几何标定的配额公式 + per-city 误差表 + Seattle/Taoyuan 留出结果。
- 对照：合成 vs 真实标定的差异（含 P1b 已观察到的"合成+真实合并重拟合反而变差"现象）。
- 贡献叙事：**从"合成走廊参数"到"公开城市数据标定"的实证升级**；宽度来源逐城
  provenance 表 + q_p prior 声明是审稿防线。
